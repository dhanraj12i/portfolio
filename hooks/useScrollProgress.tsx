import React, { useEffect, useState } from "react";

const useScrollProgress = () => {
  const [compilation, setCompilation] = useState(0);
  useEffect(() => {}, []);

  return <div>UseScrollProgress</div>;
};

export default useScrollProgress;
